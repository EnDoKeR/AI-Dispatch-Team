# Stop Provenance Fixtures

Synthetic fixtures for stop group provenance and grouping-stage tests.

These files use fake labels, fake identifiers, and redacted tokens only. They do
not contain private PDFs, screenshots, raw private text, broker names, addresses,
MC numbers, rates, references, or local paths.

